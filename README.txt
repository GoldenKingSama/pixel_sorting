The program performs pixel sorting on an input image. 
The user is prompted to provide the input image path 
and the output image path. The program then applies 
the brightness sorting algorithm to sort the pixels in 
the image. Finally, it saves the sorted image to the 
specified output image path.

Before opening the program, you need to install these 
two librarie:

	pip install Pillow
	pip install numpy
